---
import Background from '../components/Visual/Background.astro';
import Drawer from '../components/Navigation/Drawer.astro';
import DrawerTrigger from '../components/Navigation/DrawerButton.astro';
import '../styles/global.css';

interface Props {
	title?: string;
	description?: string;
}

const { 
	title = "Takuma Mineta | Portfolio", 
	description = "Creative Archive" 
} = Astro.props;
---

<!DOCTYPE html>
<html lang="ja">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
		<meta name="description" content={description} />
	</head>
	<body class="bg-black text-white overflow-x-hidden selection:bg-white/20">
		<Background />

		<Drawer side="left" id="drawer-left" />
		<Drawer side="right" id="drawer-right" />

		<DrawerTrigger side="left" targetId="drawer-left" />
		<DrawerTrigger side="right" targetId="drawer-right" />

		<main class="relative z-10 min-h-screen w-full flex flex-col items-center">
			<div class="w-full max-w-screen-xl 2xl:max-w-screen-2xl px-6 py-24 md:px-12 lg:px-24 flex flex-col items-center">
				<div class="w-full">
					<slot />
				</div>
			</div>
		</main>

		<style is:global>
			:root {
				color-scheme: dark;
			}
			html {
				scroll-behavior: smooth;
			}
			body {
				background: radial-gradient(circle at 50% 50%, #0d0d0d 0%, #000000 100%);
			}
		</style>
	</body>
</html>